version: '3.8'
services:
  textadventure:
    image: textadventure:latest
    build:
      context: .
      dockerfile: dockerfile
    ports:
      - "8080:8080"
    # Add environment variables here if needed